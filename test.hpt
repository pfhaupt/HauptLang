mem
    count 8
    index 8
    arr 8000
end

1000 count !64

while index ?64 count ?64 < do
    index ?64
    arr index ?64 8 * + !64
    index ?64 1 + index !64
end

0 index !64

while index ?64 count ?64 < do
    arr index ?64 8 * + ?64 print
    index ?64 1 + index !64
end
